import React from 'react';
import FormulaBar from 'FormulaBar';

//renders the headers
//renders the formula bar
export default class HeaderContainer extends React.Component {
  static propTypes = {
    headers: React.propTypes.list,
    showFormulaBar: React.propTypes.bool,
    showHeaderLetters: React.propTypes.bool
  };

  render(){
    return (
      <div>
        
      </div>
    );
  }
}